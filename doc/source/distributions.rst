Distribution Types
===================

Each distribution is implemented by a sub-type of ``Distribution``. The *Distributions* package organizes all distribution types in to a hierarchy as follows.

